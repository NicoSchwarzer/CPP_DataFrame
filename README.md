# C++ DataFrame

A simple C++ project that implements a **Pandas-like DataFrame** using `std::unordered_map` and `std::variant` to store columns with different data types. Includes functionality to read CSV files and inspect the data.

---

## Project Structure

CPP_DataFrame/

├── main.cpp           
├── CPP_DataFrame.cpp  
└── example_data.csv   # Sample CSV to populate the DataFrame
